{
  "name": "notify-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute notify-hub-db --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute notify-hub-db --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.6.0"
  }
}
